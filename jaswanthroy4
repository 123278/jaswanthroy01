4.	Write a c program for binary search algorithm

Program:
#include<stdio.h>
Int main()
{
     Int arr[], c, i,j,mid,n,search;
     Printf(“Enter number of elements:”);
     Scanf(“%d”,&n);
     Printf(“enter %d integers:\n,n);
     For(c=0; c<n;c++)
     {
           Scanf(“%d”,&arr[c]);
    }
     Printf(enter the value to find:\n”);
     Scanf(“%d”,&search);
    i=0;
               j= n-1;
               mid(i+j)/2;
               while(i<=j)
               {
                    If(arr[mid]< search)
                         i = mid + 1;
        	else if(arr[mid]==search {
                              printf(“ %d is present at list %d.\n”, search, mid+1)
 	break;
	}
	Else
	J = mid – 1;
	Mid = (i + j) /2;
        	}
	If(i>j)
	Printf(“%d is not found in the list.\n”, search);
	Return 0;
}

Output:
    Enter the number of elements:
     3
    Enter the 3 integers
    1
    3
    12
    Enter the value to find
  3
  3 is present at list 2

         
